{
  "name": "sfdx-wry-plugin",
  "description": "Utilities for SFDX JSON data files",
  "version": "0.0.11",
  "author": "William R. Young",
  "bugs": "https://github.com/billryoung/sfdx-wry-plugin/issues",
  "dependencies": {
    "@oclif/command": "^1",
    "@oclif/config": "^1",
    "@oclif/errors": "^1",
    "tslib": "^1"
  },
  "devDependencies": {
    "@oclif/dev-cli": "^1",
    "@oclif/plugin-help": "^2",
    "@oclif/test": "^1",
    "@salesforce/command": "^2.2.0",
    "@salesforce/core": "^2.4.0",
    "@salesforce/dev-config": "^1.4.1",
    "@salesforce/ts-types": "^1.2.2",
    "@types/chai": "^4",
    "@types/mocha": "^5",
    "@types/node": "^10.17.21",
    "chai": "^4",
    "globby": "^8",
    "mocha": "^5",
    "nyc": "^14.1.1",
    "ts-node": "^8",
    "tslint": "^5"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "files": [
    "/lib",
    "/messages",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "homepage": "https://github.com/billryoung/sfdx-wry-plugin",
  "keywords": [
    "sfdx-plugin"
  ],
  "license": "MIT",
  "oclif": {
    "commands": "./lib/commands",
    "bin": "sfdx",
    "topics": {
      "hello": {
        "description": "Commands to say hello."
      }
    },
    "devPlugins": [
      "@oclif/plugin-help"
    ]
  },
  "repository": "billryoung/sfdx-wry-plugin",
  "scripts": {
    "postpack": "rm -f oclif.manifest.json",
    "posttest": "tslint -p test -t stylish",
    "prepack": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme",
    "test": "nyc --extension .ts mocha --forbid-only \"test/**/*.test.ts\"",
    "version": "oclif-dev readme && git add README.md"
  }
}
